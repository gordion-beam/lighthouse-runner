{
  "verbose": true,
  "ignore": ["src/**/*.spec.ts"],
  "watch": ["src/**/*.ts"],
  "execMap": {
    "ts": "NODE_ENV=local && node -r ts-node/register -r dotenv/config --inspect=0.0.0.0:9229 --nolazy"
  }
}
